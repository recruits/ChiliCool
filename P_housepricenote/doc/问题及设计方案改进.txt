-------------------------------------------------------------------
--20140528
1. ProcessControlTask获取任务信息，需要把任务信息的管理移入一个单独的service中，而且需要提供线程案例的访问方法，以便多线程可以处理。
2. 任务管理类中的任务超始编号，类型不一致，需要统一成Integer
3. 所有与数据库交互的DAO接口，外面全部封装一层Service服务，然后统一使用service服务
4. 线程终止功能暂时未实现